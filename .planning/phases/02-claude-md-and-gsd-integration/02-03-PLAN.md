---
phase: 02-claude-md-and-gsd-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: true
requirements:
  - CL-01
  - CL-02
  - CL-03
must_haves:
  truths:
    - "grasshopper_csharp_learnings.md exists at project root and is the complete 2300+ line knowledge base"
    - "CLAUDE.md exists at project root and references grasshopper_csharp_learnings.md correctly"
    - "CLAUDE.md is under 100 lines"
    - "CLAUDE.md contains all three requirement areas: auto-load (CL-01), output format (CL-02), GSD workflow (CL-03)"
    - "No non-ASCII characters in CLAUDE.md"
  artifacts:
    - path: "grasshopper_csharp_learnings.md"
      provides: "Complete knowledge base from Phase 1"
      min_lines: 2300
    - path: "CLAUDE.md"
      provides: "Per-session auto-load instructions"
      contains: "Always Do First"
  key_links:
    - from: "CLAUDE.md"
      to: "grasshopper_csharp_learnings.md"
      via: "Read tool instruction"
      pattern: "Read.*grasshopper_csharp_learnings"
---

<objective>
Verify that both files are correctly placed at the project root and CLAUDE.md meets all requirements.

Purpose: Final validation that Phase 2 deliverables are complete. This is a read-only verification -- no files are created or moved. grasshopper_csharp_learnings.md was placed at the root during Phase 1; this plan confirms it is still there and that CLAUDE.md correctly references it.

Output: Verification confirmation. No file changes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-md-and-gsd-integration/02-01-SUMMARY.md
@.planning/phases/02-claude-md-and-gsd-integration/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify file placement and CLAUDE.md completeness</name>
  <files></files>
  <read_first>
    - CLAUDE.md (verify final content)
    - grasshopper_csharp_learnings.md (verify existence and line count -- read first 5 lines only to confirm, do not read entire file)
  </read_first>
  <action>
Run a comprehensive verification of Phase 2 deliverables. This is READ-ONLY -- do not create, move, or modify any files.

**Check 1 -- File existence:**
```bash
test -f "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md" && echo "PASS: CLAUDE.md exists" || echo "FAIL: CLAUDE.md missing"
test -f "D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md" && echo "PASS: knowledge base exists" || echo "FAIL: knowledge base missing"
```

**Check 2 -- CLAUDE.md line count (must be under 100):**
```bash
wc -l "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md"
```

**Check 3 -- Knowledge base line count (must be 2300+):**
```bash
wc -l "D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md"
```

**Check 4 -- CL-01 coverage (auto-load read instruction):**
```bash
grep -c "Read.*grasshopper_csharp_learnings" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md"
```
Expected: 1 or more matches.

**Check 5 -- CL-02 coverage (output format section):**
```bash
grep -c "Output Format" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md"
```
Expected: 1.

**Check 6 -- CL-03 coverage (GSD workflow section):**
```bash
grep -c "GSD Workflow" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md"
```
Expected: 1.

**Check 7 -- No inline import of large file:**
```bash
grep -c "@grasshopper_csharp_learnings" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md"
```
Expected: 0.

**Check 8 -- No non-ASCII characters:**
```bash
grep -P '[^\x00-\x7F]' "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md" || echo "PASS: ASCII only"
```

**Check 9 -- Section ordering (Always Do First is first, Permissions is last):**
```bash
head -5 "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md"
tail -3 "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md"
```

If ALL checks pass, report success. If any check fails, report which check failed and what the actual value was. Do NOT attempt to fix anything -- just report.
  </action>
  <verify>
    <automated>cd "D:/Projekte/SynologyDrive/55_C#_GH_Editor" && test -f CLAUDE.md && test -f grasshopper_csharp_learnings.md && LINE_COUNT=$(wc -l < CLAUDE.md) && KB_COUNT=$(wc -l < grasshopper_csharp_learnings.md) && HAS_READ=$(grep -c "Read.*grasshopper_csharp_learnings" CLAUDE.md) && HAS_FORMAT=$(grep -c "Output Format" CLAUDE.md) && HAS_GSD=$(grep -c "GSD Workflow" CLAUDE.md) && NO_IMPORT=$(grep -c "@grasshopper_csharp_learnings" CLAUDE.md || true) && if [ "$LINE_COUNT" -lt 100 ] && [ "$KB_COUNT" -gt 2300 ] && [ "$HAS_READ" -ge 1 ] && [ "$HAS_FORMAT" -ge 1 ] && [ "$HAS_GSD" -ge 1 ] && [ "$NO_IMPORT" -eq 0 ]; then echo "ALL CHECKS PASS"; else echo "SOME CHECKS FAILED: lines=$LINE_COUNT kb=$KB_COUNT read=$HAS_READ format=$HAS_FORMAT gsd=$HAS_GSD import=$NO_IMPORT"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at project root (D:/Projekte/SynologyDrive/55_C#_GH_Editor/)
    - CLAUDE.md is under 100 lines
    - grasshopper_csharp_learnings.md is 2300+ lines
    - CLAUDE.md contains read instruction for knowledge base (CL-01)
    - CLAUDE.md contains Output Format section (CL-02)
    - CLAUDE.md contains GSD Workflow section (CL-03)
    - CLAUDE.md does NOT use @grasshopper_csharp_learnings.md import
    - CLAUDE.md contains only ASCII characters
    - "Always Do First" is the first content section
    - "Permissions" is the last section
  </acceptance_criteria>
  <done>All 9 verification checks pass. Phase 2 deliverables are complete: CLAUDE.md auto-loads C# scripting rules (CL-01), specifies output format (CL-02), and documents GSD workflow (CL-03). Knowledge base remains intact at project root.</done>
</task>

</tasks>

<verification>
All automated checks in the verify command pass with "ALL CHECKS PASS" output.
</verification>

<success_criteria>
Phase 2 is complete when all verification checks confirm: CLAUDE.md is under 100 lines, contains all three requirement areas (CL-01, CL-02, CL-03), does not inline-import the large knowledge base, and both files exist at the project root.
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-md-and-gsd-integration/02-03-SUMMARY.md`
</output>
