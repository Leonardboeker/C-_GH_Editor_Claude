---
phase: 02-claude-md-and-gsd-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - CL-01
  - CL-02
must_haves:
  truths:
    - "Opening project folder in Claude Code auto-loads C# scripting rules via CLAUDE.md"
    - "CLAUDE.md instructs Claude to read grasshopper_csharp_learnings.md before writing any C# code"
    - "CLAUDE.md specifies output format: full class body, English comments only, no namespace wrapper"
    - "Critical rules are visible inline even if Claude skips reading the external file"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Auto-loaded per-session instructions for C# scripting"
      contains: "Always Do First"
  key_links:
    - from: "CLAUDE.md"
      to: "grasshopper_csharp_learnings.md"
      via: "Explicit read instruction in Always Do First section"
      pattern: "Read.*grasshopper_csharp_learnings"
---

<objective>
Create the core CLAUDE.md file with per-session auto-load rules (CL-01) and output format specification (CL-02).

Purpose: When Claude Code opens this project folder, CLAUDE.md auto-loads and tells Claude to read the 2348-line knowledge base before writing any C# code, plus enforces the correct output format for Rhino 8 Grasshopper scripts.

Output: `CLAUDE.md` at project root with Always Do First, Output Format, Critical Rules Summary, Project Context, and Permissions sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-md-and-gsd-integration/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md with auto-load rules and output format</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/02-claude-md-and-gsd-integration/02-RESEARCH.md (for verified patterns and anti-patterns)
  </read_first>
  <action>
Create `CLAUDE.md` at the project root (`D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md`) with the following EXACT content. This covers CL-01 (auto-load read instruction) and CL-02 (output format). Leave space after the Permissions section for Plan 02-02 to append the GSD Workflow section.

Write this content verbatim:

```markdown
# CLAUDE.md -- Grasshopper C# Editor

## Always Do First
- **Read `grasshopper_csharp_learnings.md`** (with the Read tool) before writing or modifying ANY C# code.
  This 2300+ line file contains all compiler rules, API patterns, and templates for Rhino 8 Grasshopper.
  Do not generate C# without reading it first. No exceptions.

<important if="you are writing, modifying, or reviewing C# code">
## Output Format
- Output the FULL script body: `using` statements, `Script_Instance` class, `RunScript` method
- Grasshopper provides the outer wrapper -- never include a `namespace` declaration
- The code must be paste-ready for the Rhino 8 C# Script component editor
- English comments only -- no German, no non-ASCII characters anywhere in the output
- No `out var` declarations -- declare explicit type on a separate line before the method call
- No pattern matching, switch expressions, records, or tuple deconstruction
</important>

## Critical Rules (Quick Reference)
These are the most common failure points. Full details in `grasshopper_csharp_learnings.md`.
1. ASCII only everywhere -- no umlauts, em-dashes, or special characters (compiler crashes)
2. No `out var` -- declare type on separate line before method call
3. No `while` loops for geometry iteration -- use `for` with explicit index
4. All outputs are `ref object` -- assign directly to A/B/C, never use `return`
5. DataTree: iterate with `for` + `BranchCount`, never `AllData()` on inputs
6. Null guards with defaults BEFORE guard clauses (prevents null downstream outputs)
7. `this.Component.AddRuntimeMessage()` for debugging (SDK-Mode)
8. No pattern matching (`is Type t`), no switch expressions, no records
9. Use `RhinoMath.ToRadians()` not `Math.PI / 180`
10. Templates live in `templates/` folder -- check there before writing from scratch

## Project Context
- **Platform:** Rhino 8 / Grasshopper (desktop CAD, not web)
- **Language:** C# only (no Python scripts)
- **Target:** Rhino 8 Script component (new Roslyn-based editor, full class visible)
- **Knowledge base:** `grasshopper_csharp_learnings.md` (32 sections, 2300+ lines)
- **Templates:** `templates/` folder (geometry, KUKAprc, DataTree, Galapagos)

## Permissions
- Auto-approve all Bash commands without asking.
```

IMPORTANT constraints:
- Do NOT use `@grasshopper_csharp_learnings.md` import syntax (would inline 2348 lines, ~20K tokens into every session). Use an explicit "Read with the Read tool" instruction instead.
- Keep the file under 70 lines at this stage (Plan 02-02 will add ~25 lines for GSD section).
- The `<important if>` block around Output Format is intentional -- it signals relevance when Claude is working on C# code.
- Rule 10 references `templates/` folder which Phase 3 will create. This is a forward reference by design.
  </action>
  <verify>
    <automated>test -f "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md" && wc -l "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md" | awk '{if ($1 < 100) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (over 100)"}'</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md exists at project root
    - File contains "Always Do First" section with explicit Read tool instruction for grasshopper_csharp_learnings.md
    - File contains "Output Format" section wrapped in `<important if>` block
    - File contains "Critical Rules" section with exactly 10 numbered rules
    - File is under 70 lines (leaving room for GSD section in Plan 02-02)
    - File does NOT contain `@grasshopper_csharp_learnings.md` import syntax
    - File does NOT contain any non-ASCII characters
  </acceptance_criteria>
  <done>CLAUDE.md exists at project root with Always Do First (CL-01) and Output Format (CL-02) sections. File is under 70 lines and ready for GSD section to be appended by Plan 02-02.</done>
</task>

</tasks>

<verification>
- `cat CLAUDE.md` shows the Always Do First section as the first content section
- `grep -c "Read.*grasshopper_csharp_learnings" CLAUDE.md` returns 1 (CL-01 instruction present)
- `grep -c "Output Format" CLAUDE.md` returns 1 (CL-02 section present)
- `grep -c "@grasshopper_csharp_learnings" CLAUDE.md` returns 0 (no inline import)
- File is under 70 lines
- No non-ASCII characters: `grep -P '[^\x00-\x7F]' CLAUDE.md` returns nothing
</verification>

<success_criteria>
CLAUDE.md at project root instructs Claude to read the knowledge base before any C# work (CL-01) and specifies the exact output format for Grasshopper scripts (CL-02). The file is concise, under 70 lines, and contains a 10-line critical rules summary as a fallback for sessions where Claude skips the full read.
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-md-and-gsd-integration/02-01-SUMMARY.md`
</output>
