---
phase: 02-claude-md-and-gsd-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - CL-03
must_haves:
  truths:
    - "CLAUDE.md documents GSD workflow commands for scripting tasks"
    - "User can reference /gsd:fast, /gsd:quick, and /gsd:plan-phase from CLAUDE.md"
    - "GSD section explains when to use each command with concrete examples"
  artifacts:
    - path: "CLAUDE.md"
      provides: "GSD workflow guidance section appended to existing content"
      contains: "GSD Workflow"
  key_links:
    - from: "CLAUDE.md"
      to: "GSD workflow commands"
      via: "GSD Workflow section with command reference"
      pattern: "/gsd:"
---

<objective>
Append the GSD workflow guidance section to CLAUDE.md (CL-03).

Purpose: Users need a quick reference for which GSD command to use when working on scripting tasks. This section goes at the end of CLAUDE.md (before Permissions) so the most critical rules remain at the top.

Output: Updated `CLAUDE.md` with GSD Workflow section, total file still under 100 lines.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-md-and-gsd-integration/02-RESEARCH.md
@.planning/phases/02-claude-md-and-gsd-integration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append GSD Workflow section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the file created by Plan 02-01 -- must read current content first)
    - .planning/phases/02-claude-md-and-gsd-integration/02-RESEARCH.md (for the verified GSD section pattern)
  </read_first>
  <action>
Read the existing CLAUDE.md created by Plan 02-01. Insert a new "GSD Workflow" section BEFORE the "Permissions" section (Permissions should always be the last section).

Insert the following content between the "Project Context" section and the "Permissions" section:

```markdown
## GSD Workflow
For scripting tasks in this project, use these commands:

**Quick fixes** (typos, config tweaks, 1-3 files):
```
/gsd:fast "add null guard to curve input"
```

**New scripts or small features** (single complete component):
```
/gsd:quick "create script that sorts curves by length and outputs DataTree"
```

**Complex multi-part work** (new template categories, knowledge base expansions):
```
/gsd:plan-phase N
```

**Check project status:**
```
/gsd:progress
```
```

The resulting CLAUDE.md must:
- Keep "Always Do First" as the very first section (primacy)
- Keep "Permissions" as the very last section
- Have the GSD Workflow section between Project Context and Permissions
- Stay under 100 lines total
- NOT duplicate any content from the existing sections

Use the Edit tool to insert the GSD section, do not rewrite the entire file.
  </action>
  <verify>
    <automated>test -f "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md" && grep -c "GSD Workflow" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md" | awk '{if ($1 >= 1) print "PASS: GSD section found"; else print "FAIL: GSD section missing"}' && wc -l "D:/Projekte/SynologyDrive/55_C#_GH_Editor/CLAUDE.md" | awk '{if ($1 < 100) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (over 100)"}'</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "GSD Workflow" section
    - Section documents /gsd:fast, /gsd:quick, /gsd:plan-phase, and /gsd:progress commands
    - Each command has a concrete example relevant to Grasshopper C# scripting
    - GSD section is positioned between Project Context and Permissions
    - Total file length is under 100 lines
    - "Always Do First" is still the first content section
    - "Permissions" is still the last section
  </acceptance_criteria>
  <done>CLAUDE.md contains GSD Workflow section (CL-03) with all four command references and scripting-relevant examples. File is under 100 lines total.</done>
</task>

</tasks>

<verification>
- `grep -c "/gsd:fast" CLAUDE.md` returns 1
- `grep -c "/gsd:quick" CLAUDE.md` returns 1
- `grep -c "/gsd:plan-phase" CLAUDE.md` returns 1
- `grep -c "/gsd:progress" CLAUDE.md` returns 1
- `wc -l CLAUDE.md` returns under 100
- `head -5 CLAUDE.md` shows "# CLAUDE.md" and "## Always Do First" (still first)
- `tail -3 CLAUDE.md` shows Permissions section (still last)
</verification>

<success_criteria>
CLAUDE.md now contains the complete GSD Workflow section (CL-03) with practical command examples for scripting tasks. The file remains under 100 lines with the most critical rules (Always Do First, Output Format) at the top and Permissions at the bottom.
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-md-and-gsd-integration/02-02-SUMMARY.md`
</output>
