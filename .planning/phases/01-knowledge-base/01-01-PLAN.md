---
phase: 01-knowledge-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md
autonomous: true
requirements:
  - KB-01
  - KB-05
must_haves:
  truths:
    - "Section 2 explicitly lists all prohibited syntax patterns (out var, while loops, non-ASCII) with examples"
    - "A new section documents Rhino 8 vs legacy editor differences (SDK-Mode vs Script-Mode)"
    - "Input access mode table shows Item/List/Tree with correct C# type signatures"
    - "Multiple ref object output pattern is documented with working example"
    - "Section 1 shows the full SDK-Mode class wrapper that Claude should always output"
  artifacts:
    - path: "D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md"
      provides: "Complete knowledge base with compiler rules and I/O patterns"
      contains: "## 19. Rhino 8 Script Editor vs Legacy"
  key_links:
    - from: "Section 2 (Critical Rules)"
      to: "Section 19 (Rhino 8 vs Legacy)"
      via: "Cross-reference to editor differences"
      pattern: "SDK-Mode"
---

<objective>
Copy the existing 18-section grasshopper_csharp_learnings.md into the project, then expand compiler rules (section 2), add a new Rhino 8 vs Legacy section (section 19), and expand input/output patterns (sections 3-4) with multiple-output and casting patterns.

Purpose: Establish the base document and ensure all compiler and I/O rules are comprehensive enough that Claude never produces syntax that fails in Rhino 8.
Output: grasshopper_csharp_learnings.md with sections 1-19 complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projekte/SynologyDrive/55_C#_GH_Editor/.planning/ROADMAP.md
@D:/Projekte/SynologyDrive/55_C#_GH_Editor/.planning/REQUIREMENTS.md
@D:/Projekte/SynologyDrive/55_C#_GH_Editor/.planning/phases/01-knowledge-base/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy existing file and expand compiler rules + add Rhino 8 vs Legacy section</name>
  <files>D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md</files>
  <read_first>
    - C:/Users/leona/Downloads/grasshopper_csharp_learnings (1).md
    - D:/Projekte/SynologyDrive/55_C#_GH_Editor/.planning/phases/01-knowledge-base/01-RESEARCH.md
  </read_first>
  <action>
1. Read the existing file at `C:/Users/leona/Downloads/grasshopper_csharp_learnings (1).md` (18 sections).
2. Write its full content to `D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md`.
3. Expand **Section 2 (Critical Rules)** by adding these subsections after the existing content in section 2:

```
### No `out var` Declarations
Always declare the out variable type on a separate line before the method call.
The Rhino 8 Script Editor uses Roslyn and may support `out var` in recent builds,
but older Rhino 8 installs do not. Explicit declaration is universally safe.

\`\`\`csharp
// WRONG - prohibited
// bool ok = curve.ClosestPoint(pt, out var t);

// CORRECT - explicit out declaration
double t;
bool ok = curve.ClosestPoint(pt, out t);
\`\`\`

### No Pattern Matching, Records, or Switch Expressions
Stick to classic C# control flow. These C# 8+ features may not compile
in all Rhino 8 Script Editor versions:
- `is` pattern matching (`if (obj is Point3d p)`) -- use explicit cast + null check
- `switch` expressions -- use classic `switch` statements
- record types -- use classes or structs
- tuple deconstruction (`var (a, b) = ...`) -- use explicit variable assignment

\`\`\`csharp
// WRONG - pattern matching
// if (geo is Curve c) { ... }

// CORRECT - explicit cast
Curve c = geo as Curve;
if (c != null) { /* use c */ }
\`\`\`

### String Interpolation Is Safe
String interpolation ($"text {variable}") works in Rhino 8 (Roslyn compiler).
However, keep all interpolated strings ASCII-only.

\`\`\`csharp
// OK in Rhino 8
Print($"Found {count} points");

// WRONG - non-ASCII inside interpolation
// Print($"Punkt {i} ist {distance:F2}m entfernt");
\`\`\`
```

4. Add a new **Section 19** at the end of the file:

```
## 19. Rhino 8 Script Editor vs Legacy

Rhino 8 replaced the legacy C# Script component editor with a new Script Editor.

| Feature | Legacy (Rhino 7) | New (Rhino 8) |
|---|---|---|
| Compiler | CodeDom (.NET Framework 4.x) | Roslyn (.NET 7, .NET 8 on 8.20+) |
| Editor mode | Body-only (no class visible) | Full class visible (SDK-Mode) |
| Script modes | Body-only | SDK-Mode (full class) and Script-Mode (global scope) |
| String interpolation | No | Yes |
| Async/await | No | Yes (Rhino 8.12+) |
| Target framework | .NET Framework 4.8 | .NET 7.0 (8.0 on Rhino 8.20+) |
| Auto-referenced assemblies | RhinoCommon, Grasshopper | RhinoCommon, Grasshopper, System.* |

### SDK-Mode vs Script-Mode

**SDK-Mode (use this always):** Full class with `GH_ScriptInstance` base class.
The Script Editor shows the entire class including `using` statements, class declaration,
and `RunScript` method. This is the project standard.

**Script-Mode:** Code runs in global scope without a class wrapper.
Simpler for quick tests but lacks helper method support and is harder to debug.
Do not use Script-Mode for production scripts.

### GH_ScriptInstance Available Members

Inside SDK-Mode, the following properties and methods are available on `this`:

\`\`\`csharp
// Properties available in RunScript
RhinoDocument          // Rhino.RhinoDoc - the active document
GrasshopperDocument    // GH_Document - the GH document owning this script
Component              // IGH_Component - the script component itself
Iteration              // int - how many times RunScript was called this solve

// Methods
Print(string text)                          // Output to the [out] parameter
Print(string format, params object[] args)  // Formatted output to [out]
Reflect(object obj)                         // Reflect object members to [out]
\`\`\`

### Important: Always Output Full Class

When Claude writes a C# script for Grasshopper, always output the complete
SDK-Mode class including `using` statements, class declaration, and `RunScript`.
The user pastes the entire class into the new Script Editor.
Never output just the method body -- the new editor expects a full class.
```
  </action>
  <verify>
    <automated>grep -c "## 19. Rhino 8 Script Editor vs Legacy" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md
    - Contains all original 18 sections (1-18) unchanged
    - Section 2 has new subsections: "No `out var` Declarations", "No Pattern Matching, Records, or Switch Expressions", "String Interpolation Is Safe"
    - Section 19 heading: "## 19. Rhino 8 Script Editor vs Legacy"
    - Section 19 contains SDK-Mode vs Script-Mode comparison table
    - Section 19 contains GH_ScriptInstance Available Members code block
    - Section 19 contains "Always Output Full Class" subsection
    - All code examples use explicit out declarations (no out var)
    - No non-ASCII characters anywhere in the file
  </acceptance_criteria>
  <done>Sections 1-19 complete. Section 2 expanded with out var, pattern matching, and string interpolation rules. Section 19 documents Rhino 8 vs Legacy editor differences including SDK-Mode members.</done>
</task>

<task type="auto">
  <name>Task 2: Expand input/output patterns (sections 3-4) with multiple outputs and casting</name>
  <files>D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md</files>
  <read_first>
    - D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md
    - D:/Projekte/SynologyDrive/55_C#_GH_Editor/.planning/phases/01-knowledge-base/01-RESEARCH.md
  </read_first>
  <action>
Read the file created by Task 1. Expand **Section 3 (Output Parameters)** by adding these subsections after the existing content:

```
### Multiple Outputs

Each output is a separate `ref object` parameter. Name them with the `out_` prefix
or short descriptive names matching the GH component output nicknames:

\`\`\`csharp
private void RunScript(
  List<Point3d> points,
  List<Curve> curves,
  ref object out_points,
  ref object out_curves,
  ref object out_count)
{
  if (points == null) return;
  if (curves == null) return;

  List<Point3d> resultPts = new List<Point3d>();
  List<Curve> resultCrvs = new List<Curve>();

  // ... processing ...

  // Assign ALL outputs at the end, never inside a loop
  out_points = resultPts;
  out_curves = resultCrvs;
  out_count = resultPts.Count;
}
\`\`\`

### Output Assignment Rules

1. **Assign once at the end** -- never assign `ref object` outputs inside a loop
2. **Every code path must assign** -- if you `return` early from a guard, the output stays null
3. **Lists auto-convert** -- `List<Point3d>` assigned to `ref object` automatically becomes a GH list
4. **DataTree outputs** -- assign `DataTree<T>` directly to `ref object`
5. **Single values** -- assign directly: `out_value = 42.0;`

### Default Values Before Guards

To prevent null outputs on early return, assign defaults before guards:

\`\`\`csharp
private void RunScript(List<Plane> planes, ref object out_planes, ref object out_count)
{
  // Defaults -- downstream components get these if guards trigger
  out_planes = new List<Plane>();
  out_count = 0;

  if (planes == null || planes.Count == 0) return;

  // ... actual processing ...
  out_planes = result;
  out_count = result.Count;
}
\`\`\`
```

Then expand **Section 4 (Input Access Types)** by adding after the existing table:

```
### Access Mode Reference Table

| Access Mode | C# Parameter Type | When to Use | Example Input |
|---|---|---|---|
| Item Access | `double`, `int`, `string`, `Point3d`, `Curve` | Single value per execution | Number Slider, one Point |
| List Access | `List<double>`, `List<Point3d>`, `List<Curve>` | Flat list of values | Series, multiple Points |
| Tree Access | `DataTree<object>` | Branched data, plugin outputs | KUKAprc Analysis, Entwine |

### Type Casting from Tree Access

Tree access always gives `DataTree<object>`. Cast values inside the loop:

\`\`\`csharp
private void RunScript(DataTree<object> tree, ref object A)
{
  if (tree == null || tree.BranchCount == 0) return;

  List<double> values = new List<double>();

  for (int i = 0; i < tree.BranchCount; i++)
  {
    List<object> branch = tree.Branch(i);
    for (int j = 0; j < branch.Count; j++)
    {
      // Method 1: TryParse (safest for numbers)
      double val;
      if (double.TryParse(branch[j].ToString(), out val))
      {
        values.Add(val);
      }
    }
  }
  A = values;
}
\`\`\`

### Casting Geometry from Tree Access

For geometry types, use the GH wrapper's `.Value` property:

\`\`\`csharp
// For Point3d from DataTree<object>
GH_Point ghPt = branch[j] as GH_Point;
if (ghPt != null)
{
  Point3d pt = ghPt.Value;
}

// For Curve from DataTree<object>
GH_Curve ghCrv = branch[j] as GH_Curve;
if (ghCrv != null)
{
  Curve crv = ghCrv.Value;
}

// For Plane from DataTree<object>
GH_Plane ghPln = branch[j] as GH_Plane;
if (ghPln != null)
{
  Plane pln = ghPln.Value;
}
\`\`\`

### Common Casting Types

| GH Wrapper | `.Value` Type | Use For |
|---|---|---|
| `GH_Number` | `double` | Numbers (alternative to TryParse) |
| `GH_Integer` | `int` | Integers |
| `GH_Point` | `Point3d` | Points |
| `GH_Curve` | `Curve` | Curves |
| `GH_Plane` | `Plane` | Planes |
| `GH_Surface` | `Surface` | Surfaces |
| `GH_Brep` | `Brep` | Breps |
| `GH_Vector` | `Vector3d` | Vectors |
| `GH_String` | `string` | Strings |
| `GH_Boolean` | `bool` | Booleans |
```

Write the updated file.
  </action>
  <verify>
    <automated>grep -c "### Multiple Outputs" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md" && grep -c "### Access Mode Reference Table" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md" && grep -c "### Common Casting Types" "D:/Projekte/SynologyDrive/55_C#_GH_Editor/grasshopper_csharp_learnings.md"</automated>
  </verify>
  <acceptance_criteria>
    - Section 3 contains "### Multiple Outputs" with multi-output RunScript signature example
    - Section 3 contains "### Output Assignment Rules" with 5 numbered rules
    - Section 3 contains "### Default Values Before Guards" with code example
    - Section 4 contains "### Access Mode Reference Table" with Item/List/Tree rows
    - Section 4 contains "### Type Casting from Tree Access" with TryParse code example
    - Section 4 contains "### Casting Geometry from Tree Access" with GH_Point/GH_Curve/GH_Plane examples
    - Section 4 contains "### Common Casting Types" table with 10 GH wrapper types
    - All code examples use explicit out declarations, no out var
    - No non-ASCII characters anywhere
  </acceptance_criteria>
  <done>Sections 3-4 expanded with multiple output patterns, assignment rules, default values, access mode table, geometry casting from DataTree, and GH wrapper type reference table. KB-05 fully covered.</done>
</task>

</tasks>

<verification>
- grep for "## 19." confirms new section exists
- grep for "### Multiple Outputs" confirms output expansion
- grep for "### Access Mode Reference Table" confirms input expansion
- grep for "### Common Casting Types" confirms casting reference
- Verify no non-ASCII: `LC_ALL=C grep -P '[^\x00-\x7F]' grasshopper_csharp_learnings.md` should return nothing
</verification>

<success_criteria>
- grasshopper_csharp_learnings.md exists in project root with sections 1-19
- Section 2 has 3 new subsections on prohibited syntax
- Section 3 has 3 new subsections on output patterns
- Section 4 has 3 new subsections on input casting
- Section 19 documents Rhino 8 vs Legacy with SDK-Mode details
- All code is ASCII-only, no out var, no while loops
</success_criteria>

<output>
After completion, create `.planning/phases/01-knowledge-base/01-01-SUMMARY.md`
</output>
