---
phase: 03-component-templates
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - grasshopper_csharp_learnings.md
autonomous: true
requirements: [TPL-01, TPL-02, TPL-03, TPL-04]

must_haves:
  truths:
    - "Each template section (29-32) in grasshopper_csharp_learnings.md references its standalone file path"
    - "A reader of the knowledge base can find the standalone template files via the cross-references"
  artifacts:
    - path: "grasshopper_csharp_learnings.md"
      provides: "Cross-reference links from sections 29-32 to templates/ folder"
      contains: "templates/geometry_processing.cs"
  key_links:
    - from: "grasshopper_csharp_learnings.md"
      to: "templates/*.cs"
      via: "Standalone file path references in sections 29-32"
      pattern: "Standalone file:.*templates/"
---

<objective>
Add cross-reference links from grasshopper_csharp_learnings.md template sections (29-32) to the standalone .cs files in templates/.

Purpose: So that anyone reading the knowledge base knows the templates also exist as standalone files for direct copy-paste, and CLAUDE.md's reference to the templates/ folder is backed by actual cross-links.
Output: Updated grasshopper_csharp_learnings.md with file path references in sections 29-32.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-reference links to template sections in knowledge base</name>
  <files>grasshopper_csharp_learnings.md</files>
  <read_first>grasshopper_csharp_learnings.md (sections 29-32, lines 2000-2349)</read_first>
  <action>
Add a "Standalone file:" line to each of the four template sections (29-32) in grasshopper_csharp_learnings.md. Insert the line immediately after the description paragraph, before the code block. The exact edits:

**Section 29 (line 2002):** After the description line "Copy-paste starting point for scripts that process curves, surfaces, or breps. Includes null guards, curve operations, and intersection handling." and before the code block, insert:

```
Standalone file: `templates/geometry_processing.cs`
```

**Section 30 (line 2090):** After the description line "Copy-paste starting point for scripts that generate robot toolpaths. Outputs plane list and matching speed list for KUKAprc." and before the code block, insert:

```
Standalone file: `templates/kukaprc_toolpath.cs`
```

**Section 31 (line 2203):** After the description line "Copy-paste starting point for scripts that read, transform, and output DataTrees. Preserves input tree structure in output." and before the code block, insert:

```
Standalone file: `templates/datatree_processing.cs`
```

**Section 32 (line 2288-2289):** After the description line "Copy-paste starting point for Galapagos optimization fitness functions. Must output a single number. NaN protection is critical." and before the code block, insert:

```
Standalone file: `templates/galapagos_fitness.cs`
```

Each insertion is a single line with an empty line before and after it for readability. Use the Edit tool for each insertion -- do NOT rewrite the entire file.

After edits, verify the 4 template files exist by listing the templates/ directory.
  </action>
  <verify>
    <automated>grep -c "Standalone file:" "grasshopper_csharp_learnings.md"</automated>
  </verify>
  <acceptance_criteria>
- grasshopper_csharp_learnings.md contains exactly 4 "Standalone file:" lines
- Each line points to the correct template file path
- The references appear in sections 29, 30, 31, and 32 respectively
- No other content in the file was changed
- The file still has all 2348+ lines (only 4 lines added)
  </acceptance_criteria>
  <done>grasshopper_csharp_learnings.md sections 29-32 each contain a "Standalone file:" cross-reference to the corresponding templates/*.cs file, and grep confirms exactly 4 occurrences</done>
</task>

</tasks>

<verification>
grep for "Standalone file:" in grasshopper_csharp_learnings.md returns exactly 4 matches, one per template section.
</verification>

<success_criteria>
- grasshopper_csharp_learnings.md has cross-references from all 4 template sections to templates/ folder
- The knowledge base is otherwise unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-component-templates/03-03-SUMMARY.md`
</output>
